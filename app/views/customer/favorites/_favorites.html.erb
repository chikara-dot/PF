 <% if current_customer.present?%>
  <% if post.favorited_by?(current_customer)%>
    <%= link_to customer_genre_category_post_favorites_path(category.genre_id,category,post), method: :delete, remote: true do %>
      ♥<%= post.favorites.count %>いいね
    <% end %>
  <% else %>
    <%= link_to customer_genre_category_post_favorites_path(category.genre_id,category,post), method: :post, remote: true do %>
      ♡<%= post.favorites.count %>いいね
    <% end %>
  <% end %>
<% else %>
 <%= post.favorites.count %>いいね
<% end %>
