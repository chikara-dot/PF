<div class="container">
 <div class="row">
  <h2 class="mt-3 h1">新規登録</h2>


      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <table class="table ml-5">
        <%= render "customers/shared/error_messages", resource: resource %>
      <tbody>
       <tr>
        <td><%= f.label "名前" %><br></td>
        <td><%= f.text_field :last_name, autofocus: true, placeholder: "(姓) 山田" %>
        <%= f.text_field :first_name, placeholder: "（名）花子" %></td>
       </tr>

        <tr>
         <td><%= f.label "フリガナ"%><br></td>
         <td><%= f.text_field :last_name_kana, placeholder: "（セイ）ヤマダ"%>
         <%= f.text_field :first_name_kana, placeholder: "（ナ）ハナコ"%></td>
        </tr>

        <tr>
          <td><%= f.label :email %><br /></td>
          <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
        </tr>

        <tr>
         <td><%= f.label "パスワード(6文字以上)" %></td>
         <td><%= f.password_field :password, autocomplete: "new-password" %></td>
        </tr>
        <tr>
         <td><%= f.label "パスワード(確認用)" %></td>
         <td><%= f.password_field :password_confirmation, autocomplete: "new-password" %></td>
        </tr>

       <tr>
         <td><h4>プロフィール</h4><h5>※ニックネーム以外は入力しなくても大丈夫です！後で変更できます！</h5></td>
       </tr>

        <tr>
         <td><%= f.label "ニックネーム"%></td>
         <td><%= f.text_field :nickname, placeholder: "サイト上で表示される名前です", size: "30*20"%></td>
        </tr>

        <tr>
         <td><%= f.label "自己紹介"%></td>
         <td><%= f.text_field :body, placeholder: "記載しなくても大丈夫です", size: "30*20" %></td>
        </tr>

        <tr>
         <td><%= f.label "アイコン"%></td>
         <td><%= f.attachment_field :image %></td>
        </tr>

          <tr><td><%= f.submit "新規登録！" %></td></tr>

      </tbody>
  </table>
      <% end %>
  <div>
   既に登録済みの方は</br>
   <%= link_to "こちら", new_customer_session_path %>からログインしてください
  </div>
 </div>
</div>
