<div class="col-lg-12 text-right"><%= link_to "", new_customer_genre_category_path, class: "fas fa-plus fa-5x"%></div>

<% @categories.each do |category| %>
 <%= attachment_image_tag category, :image, size: "400x300" %>
 <%= link_to category.name, customer_genre_category_posts_path(category.genre_id,category) %>
<% end %>